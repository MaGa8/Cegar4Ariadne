ROOTDIR = ..
TESTDIRS = $(TREE_DIR) $(GRAPH_DIR)
MODULES = $(TEST_RUNNER_DIR) $(TESTDIRS) $(addsuffix /test,$(TESTDIRS))
TARGET = runAllTests

OPTFLAGS = -g -Wall -std=c++17

-include $(ROOTDIR)/common.mk
