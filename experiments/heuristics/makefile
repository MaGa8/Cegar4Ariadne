ROOTDIR = ../..
TESTDIRS = 
MODULES = $(TREE_DIR) $(GRAPH_DIR) $(REFINEMENT_DIR) $(UTIL_DIR)
TARGET = heuristics

INCFLAGS ?= -isystem $(ARIADNE_DIR) -isystem $(ARIADNE_DIR)/source
LDFLAGS ?= -L $(ARIADNE_DIR)/build
LDLIBS ?= -l ariadne

-include $(ROOTDIR)/common.mk
